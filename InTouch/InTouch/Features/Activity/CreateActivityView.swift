import SwiftUI

struct CreateActivityView: View {
    var body: some View {
        NavigationStack {
            Text("Create Activity — coming soon")
                .navigationTitle("New Activity")
        }
    }
}
