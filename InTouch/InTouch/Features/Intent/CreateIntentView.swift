import SwiftUI

struct CreateIntentView: View {
    var body: some View {
        NavigationStack {
            Text("Create Intent — coming soon")
                .navigationTitle("New Intent")
        }
    }
}
