import SwiftUI

struct DiscoveryFeedView: View {
    var body: some View {
        NavigationStack {
            Text("Discovery Feed — coming soon")
                .navigationTitle("Discover")
        }
    }
}
