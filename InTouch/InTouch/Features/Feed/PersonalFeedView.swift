import SwiftUI

struct PersonalFeedView: View {
    var body: some View {
        NavigationStack {
            Text("Personal Feed — coming soon")
                .navigationTitle("Feed")
        }
    }
}
